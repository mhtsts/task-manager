apiVersion: v1
kind: Service
metadata:
  name: task-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: task-service
    chart: task-manager
spec:
  type: ClusterIP
  selector:
    app: task-service
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.task-service.port }}
      targetPort: {{ .Values.task-service.port }}